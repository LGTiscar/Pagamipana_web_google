# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (never commit real keys)
.env
.env.*
!.env.example
# .env.production SÍ se commitea: solo contiene valores PÚBLICOS (URL + publishable key),
# que de todas formas van incrustados en el bundle que se sirve al navegador.
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
